<div class="survey-map__container">
  <%= cell(
    "decidim/locations/map",
    Array(response.question),
    map_display: true,
    type: "display"
  ) %>
</div>

<% if response.question.allow_comments %>
  <div>
    <%= response_form.text_area :body, label: false, id: field_id, rows: 10, disabled:, maxlength:, class: "w-full", required: false %>
  </div>
<% end %>

<%= append_stylesheet_pack_tag "decidim_forms_locations" %>
